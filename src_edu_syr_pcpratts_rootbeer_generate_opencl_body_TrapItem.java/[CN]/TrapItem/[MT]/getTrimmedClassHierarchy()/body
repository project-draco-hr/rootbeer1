{
  List<SootClass> all=OpenCLScene.v().getClassHierarchy(m_Exception);
  List<SootClass> ret=new ArrayList<SootClass>();
  for (int i=0; i < all.size(); ++i) {
    SootClass curr=all.get(i);
    ret.add(curr);
    if (curr.equals(m_Exception))     break;
  }
  return ret;
}
