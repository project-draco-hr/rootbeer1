{
  Token ret;
  if (m_Putbacks.isEmpty() == false) {
    ret=m_Putbacks.get(0);
    m_Putbacks.remove(0);
    return ret;
  }
  if (shouldSplice()) {
    read();
    if (m_LowerLevel.hasNext())     read();
    ret=new Token(" ");
  }
 else {
    ret=m_Curr;
    read();
  }
  return ret;
}
