{
  int[] result=null;
  for (int i=0; i < 500; ++i) {
    int len=2000;
    int[] values=backrdsArray(len);
    quicksort(values,0,len - 1);
    if (result == null) {
      result=values;
    }
  }
  printArray("cpu_result",result);
}
