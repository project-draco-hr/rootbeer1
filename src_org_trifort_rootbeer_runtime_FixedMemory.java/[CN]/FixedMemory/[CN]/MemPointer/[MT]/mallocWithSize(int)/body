{
  int mod=size % 16;
  if (mod != 0)   size+=(16 - mod);
  long ret=m_endPointer;
  m_endPointer+=size;
  if (ret + size > m_size) {
    throw new OutOfMemoryError();
  }
  m_pointer=ret;
  if (ret > m_heapEnd)   m_heapEnd=ret;
  return ret;
}
