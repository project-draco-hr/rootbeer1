{
  Chain<Local> locals=m_body.getLocals();
  String ret="";
  for (  Local local : locals) {
    OpenCLType type=new OpenCLType(local.getType());
    ret+=type.getCudaTypeString() + " " + local.getName();
    if (type.isRefType())     ret+=" = -1";
    ret+=";\n";
  }
  return ret;
}
