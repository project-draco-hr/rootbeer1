{
  Set<String> key_set=mLabelToUnitBoxMap.keySet();
  for (  String key : key_set) {
    List<UnitBox> boxes=mLabelToUnitBoxMap.get(key);
    for (    UnitBox box : boxes) {
      if (box.getUnit() == null)       throw new RuntimeException("box unit is null!");
    }
  }
}
