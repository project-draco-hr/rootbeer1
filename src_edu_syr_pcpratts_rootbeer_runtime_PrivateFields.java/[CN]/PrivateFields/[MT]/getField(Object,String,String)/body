{
  try {
    Class cls=Class.forName(cls_name);
    Field f=cls.getDeclaredField(name);
    f.setAccessible(true);
    return f;
  }
 catch (  Exception ex) {
    ex.printStackTrace();
    return null;
  }
}
