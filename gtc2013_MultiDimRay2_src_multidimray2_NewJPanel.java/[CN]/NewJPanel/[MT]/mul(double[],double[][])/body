{
  double[] result=new double[v.length];
  for (int a=0; a < v.length; ++a) {
    double x=0;
    for (int b=0; b < v.length; ++b) {
      x+=v[b] * matrix[a][b];
    }
    result[a]=x;
  }
  return result;
}
