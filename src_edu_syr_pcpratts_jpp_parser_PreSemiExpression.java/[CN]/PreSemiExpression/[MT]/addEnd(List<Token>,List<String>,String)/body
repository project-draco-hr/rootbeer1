{
  for (  String curr : number_tokens) {
    if (curr.equals(splitter) == false)     return;
    Token new_token=new Token(splitter);
    new_token.setImmutable();
    new_tokens.add(new_token);
  }
}
