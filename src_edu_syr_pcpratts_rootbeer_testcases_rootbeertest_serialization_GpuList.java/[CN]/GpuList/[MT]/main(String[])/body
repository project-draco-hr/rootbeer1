{
  GpuList<Integer> int_list=new GpuList<Integer>();
  for (int i=0; i < 40; ++i)   int_list.add(i);
  int_list.remove(20);
  for (int i=0; i < 39; ++i) {
    System.out.println(int_list.get(i));
  }
}
