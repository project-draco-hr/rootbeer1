{
  int[] array=rhs.m_array;
  for (int i=0; i < array.length; ++i) {
    if (array[i] != i) {
      System.out.println("failure at: " + i + " array[i]: "+ array[i]);
      return false;
    }
  }
  return true;
}
