{
  for (  Type type : hierarchy) {
    if (type instanceof RefType == false) {
      continue;
    }
    RefType ref_type=(RefType)type;
    SootClass soot_class=ref_type.getSootClass();
    if (sootClassHasMethod(soot_class) == false)     continue;
    return soot_class;
  }
  return null;
}
