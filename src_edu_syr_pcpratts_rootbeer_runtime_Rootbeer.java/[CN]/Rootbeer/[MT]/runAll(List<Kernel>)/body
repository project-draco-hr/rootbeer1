{
  if (jobs.isEmpty()) {
    return;
  }
  if (jobs.get(0) instanceof CompiledKernel == false) {
    for (    Kernel job : jobs) {
      job.gpuMethod();
    }
  }
 else {
    m_stats=new ArrayList<StatsRow>();
    m_Rootbeer.runAll(jobs);
  }
}
