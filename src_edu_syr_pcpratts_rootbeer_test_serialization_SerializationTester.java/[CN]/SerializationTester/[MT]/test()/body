{
  try {
    List<Kernel> objects=m_Tester.create();
    write(objects);
    if (readAndTest(objects) == false)     return false;
    return true;
  }
 catch (  Exception ex) {
    fail();
    ex.printStackTrace();
    return false;
  }
}
