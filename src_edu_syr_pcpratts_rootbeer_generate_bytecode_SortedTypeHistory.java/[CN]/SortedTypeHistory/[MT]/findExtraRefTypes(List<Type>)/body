{
  List<Type> ret=new ArrayList<Type>();
  for (  Type type : types) {
    ret.add(type);
    if (type instanceof ArrayType == false)     continue;
    ArrayType array_type=(ArrayType)type;
    if (array_type.baseType instanceof RefType)     ret.add(array_type.baseType);
  }
  return ret;
}
