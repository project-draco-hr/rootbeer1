{
  List<TreeNode> children=curr.getChildren();
  if (children.isEmpty())   return len;
  int max_len=len;
  for (  TreeNode child : children) {
    int new_len=getLen(child,len + 1);
    if (new_len > max_len)     max_len=new_len;
  }
  return max_len;
}
