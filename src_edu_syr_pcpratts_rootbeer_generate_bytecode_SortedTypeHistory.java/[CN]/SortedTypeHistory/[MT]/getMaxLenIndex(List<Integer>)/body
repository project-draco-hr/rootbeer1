{
  int max=-1;
  int max_index=-1;
  for (int i=0; i < lengths.size(); ++i) {
    int len=lengths.get(i);
    if (len < 1)     continue;
    if (len > max) {
      max=len;
      max_index=i;
    }
  }
  return max_index;
}
