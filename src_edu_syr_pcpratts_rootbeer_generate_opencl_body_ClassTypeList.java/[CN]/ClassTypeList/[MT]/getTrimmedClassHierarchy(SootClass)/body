{
  List<SootClass> all=OpenCLScene.v().getClassHierarchy(soot_class);
  List<SootClass> ret=new ArrayList<SootClass>();
  for (int i=0; i < all.size(); ++i) {
    SootClass curr=all.get(i);
    ret.add(curr);
    if (curr.equals(soot_class))     break;
  }
  return ret;
}
