{
  Token token;
  while (true) {
    token=m_LowerLevel.next();
    if (token.isNewLine() == false) {
      break;
    }
    if (m_LowerLevel.hasNext() == false) {
      return;
    }
  }
  m_Tokens.add(token);
}
